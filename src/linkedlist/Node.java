package linkedlist;

public class Node { //1 node contains value and pointer to next node
    int val;
    Node next;
    public Node(int N) { //constructor initialization
        val=N;
        next=null;
    }
}
